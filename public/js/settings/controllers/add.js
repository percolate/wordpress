'use strict';

angular.module('myApp')
  .controller('AddCtr', function ($scope, $rootScope) {
    console.log('Add New Channel state')

    /*
     * Object to hold all the data for the new channel
     */
    $scope.activeChannel = {}


  })
